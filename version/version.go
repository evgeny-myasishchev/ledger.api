package version

// VERSION is the app-global version string
// set in Makefile at build time
var VERSION = "0.0.0-dev"

// AppName name of the service
var AppName = "ledger-api"

// GitHash injected build time (see Makefile)
var GitHash = "TBD"

// GitRef injected build time (see Makefile)
var GitRef = "TBD"

// GitURL injected build time (see Makefile)
var GitURL = "TBD"
